node ("$node") {

   stage('SCM') {
      // git clone for web hooks
	  git '$PROJECT_URL'
   }
   
   stage ('build the packages') {
      // mvn package web hooks
	  sh '$ACTIVITY'
   }

    stage ('archival') {
     // archiving arti_facts
	 archive 'target/*.jar'
   }

}
