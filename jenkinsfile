node ("$node") {

   stage('SCM') {
      // git clone for testing purpose
	  git '$PROJECT_URL'
   }
   
   stage ('build the packages') {
      // mvn package for testing purpose
	  sh '$ACTIVITY'
   }

    stage ('archival') {
     // archiving arti_facts
	 archive 'target/*.jar'
   }

}
