node ("$node") {

   stage('SCM') {
      // git clone this modification for webhook testing purpose
	  git '$PROJECT_URL'
   }
   
   stage ('build the packages') {
      // mvn package this modification for webhook testing purpose
	  sh '$ACTIVITY'
   }

    stage ('archival') {
     // archiving arti_facts
	 archive 'target/*.jar'
   }

}
