node ("$node") {

   stage('SCM') {
      // git clone this modification for webhook testing
	  git '$PROJECT_URL'
   }
   
   stage ('build the packages') {
      // mvn package this modification for webhook testing
	  sh '$ACTIVITY'
   }

    stage ('archival') {
     // archiving arti_facts
	 archive 'target/*.jar'
   }

}
